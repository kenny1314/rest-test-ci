name: Java CI with Gradle

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

#env:
#  IMAGE_NAME: rest-test-master

jobs:
  deploy:

    runs-on: ubuntu-latest

    steps:
      - name: Deploy Digital Ocean
        uses: actions/setup-java@v3
        with:
          host: ${{secrets.SSH_HOST}}
          key: ${{secrets.SSH_KEY}}
          username: ${{secrets.SSH_USERNAME}}
          java-version: '11'

          script: echo 'hello igor by Digital Ocean'